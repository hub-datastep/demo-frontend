version: "3"

services:
  client-demo:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: client-demo
    ports:
      - "3003:3003"
    env_file:
      - .env.production
    volumes:
      - ./ngnix.conf:/etc/nginx/conf.d/default.conf
