version: "3"

services:
  client-demo:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: client-demo
    ports:
      - "3003:3003"
    env_file:
      - .env
    volumes:
      - ./ngnix.conf:/etc/nginx/conf.d/default.conf
